# CLAUDE.md

## 项目简介

这是一个 **Phaser 3 游戏开发学习项目**（练手项目）。项目的目标不仅是做出一个游戏，更重要的是在过程中学习游戏开发的核心概念、积累心得。

## 技术栈

- **游戏框架**：Phaser 3.60（通过 CDN 引入，非 npm 包）
- **开发服务器**：`serve`（npm run dev → http://localhost:3000）
- **语言**：纯 JavaScript（无 TypeScript、无打包工具）
- **物理引擎**：暂未启用（计划使用 Arcade）

## 项目结构

```
firstgame/
├── index.html              # 入口 HTML，加载 Phaser CDN + 场景文件
├── js/
│   ├── main.js             # Phaser.Game 配置和启动
│   └── scenes/
│       ├── HomeScene.js    # 首页场景（标题、开始按钮、纹理生成）
│       └── GameScene.js    # 游戏主场景（点阵展示、Container 用法）
├── docs/
│   └── devlog/             # 开发日志（学习心得、踩坑记录）
├── package.json
└── CLAUDE.md               # 本文件
```

## 运行方式

```bash
npm run dev        # 启动开发服务器 → http://localhost:3000
```

## 用户的学习偏好

1. **中文交流**：所有对话和文档使用中文
2. **理解原理优先**：不只是"怎么做"，更要理解"为什么这么做"和"背后发生了什么"
3. **教学式注释**：代码中保留了大量注释，这些注释本身就是知识沉淀的一部分。AI 也应该沿用这种风格写注释
4. **渐进式学习**：从简单概念开始，逐步深入。不要一次性引入太多新概念
5. **动手实践**：用户倾向于先理解概念，然后实际写代码验证

## AI 助手指引

### 沟通风格
- 用中文回答，语气耐心、友好
- 解释概念时给出类比或图示，帮助建立直觉理解
- 不要假设用户已经知道某个概念，必要时主动解释

### 代码风格
- 新代码沿用现有注释风格：分段清晰、解释"为什么"、标注关键参数含义
- 在关键设计决策处加 `// NOTE:` 标签，方便日后回顾
- 不要为了"代码简洁"而删减注释——注释本身就是学习产出

### 知识沉淀
- 每次开发会话结束时，主动询问用户："今天有什么值得记录到开发日志的吗？"
- 如果用户分享了一个重要的心得或"aha moment"，主动帮他们提炼并建议写入日志
- 维护 `docs/devlog/README.md` 索引，新增日志后更新索引

### 技术约束
- 项目使用 Phaser 3.60 CDN 版本，API 以 3.60 的文档为准
- 没有模块打包工具，所有 JS 文件通过 `<script>` 标签加载
- 场景类必须挂在全局作用域（因为 main.js 的 config.scene 数组需要引用它们）

## 知识沉淀体系

本项目采用三层沉淀结构：

1. **代码注释**（`js/**/*.js`）— 最贴近代码的知识，解释概念和决策
2. **开发日志**（`docs/devlog/YYYY-MM-DD.md`）— 每次开发的学习心得、踩坑记录
3. **CLAUDE.md**（本文件）— 项目的元信息，随项目演进而更新
