/**
 * config.js — 游戏所有可调参数
 *
 * 【怎么用】
 *   所有场景和 main.js 都能直接用，因为 config.js 最先加载。
 *   想改什么来这里翻就行，不用在代码里找了。
 *
 * 【命名规则】
 *   GAME    → 游戏全局设定
 *   MATRIX  → 点阵相关
 *   TEX     → 纹理（纹理）尺寸和颜色
 *   SCENE   → 场景背景色
 */

const CONFIG = {

    // =========================================================================
    // GAME — 游戏全局
    // =========================================================================
    GAME: {
        WIDTH: 390,             // 画布宽度
        HEIGHT: 844,            // 画布高度
        BG: '#000000',          // 画布背景色（场景覆盖之前会露出）
    },

    // =========================================================================
    // MATRIX — 点阵
    // =========================================================================
    MATRIX: {
        COLS: 5,                // 列数（横向）
        ROWS: 6,                // 行数（纵向）
        SPACING: 25,            // 点间距（圆心到圆心）
    },

    // =========================================================================
    // TEX — 纹理（GenerateTexture 生成的图）
    // =========================================================================
    TEX: {
        // 按钮
        BTN_W: 200,                // 按钮宽
        BTN_H: 60,                 // 按钮高
        BTN_RADIUS: 12,            // 按钮圆角
        BTN_COLOR: 0x4488ff,       // 按钮颜色（蓝）

        // 方块
        BLOCK_SIZE: 40,            // 方块边长
        BLOCK_COLOR: 0x00cc88,     // 方块颜色（绿）

        // 大圆
        CIRCLE_RADIUS: 20,         // 圆半径（纹理尺寸 = 半径×2）
        CIRCLE_COLOR: 0xff6644,    // 圆颜色（橙）

        // 点阵圆点
        DOT_RADIUS: 1.5,             // 圆点半径
        DOT_COLOR: 0xcccccc,       // 圆点颜色（浅灰）

        // 箭头（一格长，指向右）
        ARROW_COLOR: 0x1A1A1A,     // 箭头颜色（深黑灰）

        // 线段（无箭头，一格长，用于箭头线中间段）
        SEGMENT_COLOR: 0x1A1A1A,   // 线段颜色（深黑灰，和箭头一致）
    },

    // =========================================================================
    // ARROW_LINE — 箭头线飞出动画
    // =========================================================================
    ARROW_LINE: {
        STEP_DURATION: 50,   // 每移动一格的时间（ms）
    },

    // =========================================================================
    // ARROW_MATRIX — 箭头阵生成参数
    // =========================================================================
    ARROW_MATRIX: {
        TERMINATE_PROB: 0.05,  // 随机游走每步终止的概率（0~1，越小线越长）
    },

    // =========================================================================
    // SCENE — 场景背景色
    // =========================================================================
    SCENE: {
        HOME_BG: 0xE7E9F2,         // 首页背景（浅灰蓝）
        GAME_BG: 0xE7E9F2,         // 游戏场景背景（浅灰蓝）
    },
};
